const Loading = () => {
    return (
        <main>
            <section className=" h-screen flex items-center justify-center bg-base-300">
                 <span className="loading loading-spinner text-primary"></span>
            </section>
        </main>
    )
}

export default Loading
